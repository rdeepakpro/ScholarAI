# Security Policy

Security fixes currently target the latest development release.

Report vulnerabilities privately to the repository owner rather than opening a public issue. Include affected versions, reproduction steps, and impact. Do not include real API keys, private study material, or downloaded model files.

SpeedyAI binds local inference to `127.0.0.1`, isolates the renderer, disables Node integration, and keeps secrets and user data out of the repository.
